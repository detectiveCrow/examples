### Dependency

- pipenv
- fastapi
- uvicorn
- motor : MongoDB Driver

### Command History

```
# install
pip3 install pipenv
pipenv shell
pipenv install -r requirements.txt

# run server
uvicorn main:app --reload
```
